# artemyx_web — environment template
#
# This site is a static brochure with no runtime config.
# No environment variables are required for local development or deployment.
#
# If runtime config is added in the future, document the keys here (no values).
